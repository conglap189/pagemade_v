/**
 * CONTENT BLOCKS - Category: Content
 * Custom blocks for Content category (adds to existing plugin blocks)
 */

export default function(editor) {
  console.log('📦 Content blocks module ready (no custom blocks yet)');
  
  // Add custom Content blocks here
}
