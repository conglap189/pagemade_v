/**
 * COMMERCE BLOCKS - Category: Commerce
 * Custom blocks for Commerce category (adds to existing plugin blocks)
 */

export default function(editor) {
  console.log('📦 Commerce blocks module ready (no custom blocks yet)');
  
  // Add custom Commerce blocks here
}
