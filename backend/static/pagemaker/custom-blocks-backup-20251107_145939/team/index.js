/**
 * TEAM BLOCKS - Category: Team
 * Custom blocks for Team category (adds to existing plugin blocks)
 */

export default function(editor) {
  console.log('📦 Team blocks module ready (no custom blocks yet)');
  
  // Add custom Team blocks here
}
