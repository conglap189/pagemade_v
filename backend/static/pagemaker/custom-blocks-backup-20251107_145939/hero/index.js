/**
 * HERO BLOCKS - Category: Hero
 * Custom blocks for Hero category (adds to existing plugin blocks)
 */

export default function(editor) {
  console.log('📦 Hero blocks module ready (no custom blocks yet)');
  
  // Add custom Hero blocks here
}
