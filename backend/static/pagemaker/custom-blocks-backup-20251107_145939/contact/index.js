/**
 * CONTACT BLOCKS - Category: Contact
 * Custom blocks for Contact category (adds to existing plugin blocks)
 */

export default function(editor) {
  console.log('📦 Contact blocks module ready (no custom blocks yet)');
  
  // Add custom Contact blocks here
}
