/**
 * GALLERY BLOCKS - Category: Gallery
 * Custom blocks for Gallery category (adds to existing plugin blocks)
 */

export default function(editor) {
  console.log('📦 Gallery blocks module ready (no custom blocks yet)');
  
  // Add custom Gallery blocks here
}
