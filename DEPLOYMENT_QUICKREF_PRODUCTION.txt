# Production Deployment - Quick Reference

## 🚀 Current Status

✅ **LIVE**: http://pagemade.site  
⚠️ **HTTPS**: Pending SSL configuration (see below)

---

## 🔐 Enable HTTPS (Choose ONE)

### Option 1: Quick (2 minutes) - Cloudflare Flexible SSL
```
1. Open: https://dash.cloudflare.com
2. Select: pagemade.site
3. Go to: SSL/TLS → Overview
4. Set: Flexible
5. Done! Visit: https://pagemade.site
```

### Option 2: Secure (5 minutes) - Cloudflare Full SSL
```bash
# On local machine
cd /home/helios/ver1.1
./setup_cloudflare_ssl.sh

# Then in Cloudflare Dashboard:
# SSL/TLS → Overview → Set to "Full"
```

---

## 🔧 Common Tasks

### Restart Application
```bash
ssh root@36.50.55.21
systemctl restart pagemade
```

### View Logs
```bash
ssh root@36.50.55.21
journalctl -u pagemade -f
```

### Check Status
```bash
ssh root@36.50.55.21
systemctl status pagemade
systemctl status nginx
curl -I http://localhost:5000/
```

### Deploy Updates
```bash
# Local
cd /home/helios/ver1.1
tar -czf /tmp/pagemade-deploy.tar.gz \
    --exclude='.git' --exclude='node_modules' \
    backend/ docs/ README.md
./upload_to_vps.sh

# VPS
ssh root@36.50.55.21
cd /var/www/pagemade
tar -xzf /tmp/pagemade-deploy.tar.gz
systemctl restart pagemade
```

---

## 📁 Important Paths

| Item | Path |
|------|------|
| Application | `/var/www/pagemade/backend/` |
| Database | `/var/www/pagemade/backend/app.db` |
| Logo files | `/var/www/pagemade/backend/static/images/branding/` |
| Nginx config | `/etc/nginx/sites-available/pagemade` |
| Service file | `/etc/systemd/system/pagemade.service` |

---

## 🆘 Emergency

### Site Down?
```bash
ssh root@36.50.55.21
systemctl restart pagemade
systemctl restart nginx
```

### Database Issues?
```bash
ssh root@36.50.55.21
cd /var/www/pagemade/backend
ls -lh app.db  # Check if exists
chmod 644 app.db  # Fix permissions if needed
```

---

## 📞 Access Info

**VPS**: `ssh root@36.50.55.21` (port 22)  
**Site**: http://pagemade.site  
**IP**: http://36.50.55.21  

**Service Commands**:
- `systemctl status pagemade` - Check app status
- `systemctl restart pagemade` - Restart app
- `nginx -t` - Test Nginx config
- `systemctl reload nginx` - Reload Nginx

---

**For detailed info, see**: `PRODUCTION_DEPLOYMENT_SUMMARY.md`
